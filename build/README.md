# Build folder
Here HDD image(s) is built.  
Content of this folder should be copied to MiSTer SD card.

Put A1200 Kickstart ROM rev 40.68 as `KICK.ROM` in `AmigaGameSelector30`
If not extracted from AGS HDF files.